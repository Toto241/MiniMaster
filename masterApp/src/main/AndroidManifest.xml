<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Support different screen sizes and densities -->
    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true" />

    <application
        android:name=".MasterApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Firebase Cloud Messaging Service for receiving push notifications -->
        <service
            android:name=".MasterFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!--
          Debug diagnostic interface – only reachable via ADB (USB + USB-debugging).
          android:exported="false" prevents any external app from sending these broadcasts.
          The session requires challenge-response HMAC authentication (see DebugSessionManager).
        -->
            <receiver
                android:name=".debug.DebugBroadcastReceiver"
                android:exported="true">
            <intent-filter>
                <action android:name="com.minimaster.masterapp.DEBUG_GET_CHALLENGE" />
                <action android:name="com.minimaster.masterapp.DEBUG_ACTIVATE" />
                <action android:name="com.minimaster.masterapp.DEBUG_DEACTIVATE" />
                <action android:name="com.minimaster.masterapp.DEBUG_DUMP_STATE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
