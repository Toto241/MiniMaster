name: Android CI
on:
  push:
    paths: ['**/*.kt', '**/build.gradle', 'settings.gradle', 'gradle/**']
  pull_request:
    paths: ['**/*.kt', '**/build.gradle', 'settings.gradle', 'gradle/**']

jobs:
  android:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    defaults:
      run:
        # Set working directory to repository root where Gradle files are located
        working-directory: .
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Validate Gradle Wrapper
        uses: gradle/gradle-build-action@v3
        with:
          validate-wrapper: true
      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Gradle cache
        uses: gradle/gradle-build-action@v3
      - name: Show Gradle version
        run: ./gradlew --version
      - name: Build & Unit tests
        run: ./gradlew clean testDebugUnitTest :masterApp:assembleDebug :childApp:assembleDebug
      - name: Lint
        run: ./gradlew lint
      - name: Android test (instrumentation)
        run: echo "Instrumentation tests are typically run on emulator; configure matrix if needed."
      - name: Jacoco report
        run: ./gradlew jacocoTestReport || echo "Jacoco not configured, skipping coverage"
      - name: Upload reports
        uses: actions/upload-artifact@v4
        with:
          name: android-reports
          path: |
            **/build/reports/**
            **/build/jacoco/**