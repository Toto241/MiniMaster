processors:
  active: true

config:
  validation: true

build:
  maxIssues: 0

style:
  MagicNumber:
    active: false
  WildcardImport:
    active: false
  
complexity:
  LongMethod:
    threshold: 60
  LongParameterList:
    functionThreshold: 8
    
potential-bugs:
  active: true
  
performance:
  active: true
  
coroutines:
  active: true