/**
 * Placeholder for debug related UI. Currently the debug panel is part of
 * support.js, but if additional debug pages are required they can be
 * implemented here. Keeping a separate module allows modular growth.
 */
export function showDebugPanel(container) {
  container.innerHTML = '<h2>Debug</h2><p>Debugging-Tools werden noch umgesetzt.</p>';
}